CIMT   CHANGE DIMENSION OF RANK TO 2 X MQUAL
CIMQP  CHANGE STOTAL TO INCLUDE TOTAL OUTFLOW LOAD ALSO
      COMMON/NEWTR/RANK(NQP,2*MQUAL),ZUM(30),SURLEN(NET),QR(NET),
     1             SBIG(NET),SPEAK(NET),ISTIME(NET,2),JSTIME(NET,2),
     2             KMESS(MQUAL),SMEAN(NET),KITER(NET),KITMAX(NET),
     3             EMAX(NET),QBIG(NET),STOTAL(NET,0:MQUAL),RANQ(NET),
     4             SMAL(NET),NODSGN(NET),IFLIP,ISLOPE,INFLEW
CIMT   ADD COMMON FOR MANHOLES TO HANDLE ADDITIONAL CONSTITUENT CONCENTRATIONS
      COMMON/NEWMN/PMANN(NET,MQUAL)   
CIMQP  ADD COMMON FOR QUALITY SPLITTER MANHOLE
CIMQP   DRATIO IS THE DELIVERY RATIO
CIMQP   XNT27 STORES MASS LOST TO FLOW SPLIT
CIMQP   TOTALS FOR COMPLETE SYSTEM SUMMED TO XNT27(NE+1,IP)
      COMMON/NEWQP/DRATIO(NET,MQUAL),XNT27(NET,MQUAL)
