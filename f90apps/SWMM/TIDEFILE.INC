CIMTIME   NEW COMMON BLOCKS FOR READING TIDES FROM ASCII FILE
      PARAMETER (MAXTIDES=100,MAXDUP=100)
      COMMON /NEWTIDE1/NTDN,NTIDS,NDUP,
     1JTIDS(MAXTIDES),JDUP(MAXDUP,2),
     2ATIDS(MAXTIDES),ADUP(MAXDUP,2)
      character*10 atids,adup
      COMMON /NEWTIDE2/TIDDATE(2),tideel(maxtides,2)
      DOUBLE PRECISION TIDDATE
