
c     begin include file cchtrl.inc

c     + + + COMMON BLOCK DESCRIPTION + + +
c     + + + INSERT DESCRIPTION HERE  + + +

      common /chtrl/ icntrl(mxplan), ctlz(mxplan), ctln(mxplan),
     1    ctlslp(mxplan), rccoef(mxplan), rcexp(mxplan),
     1    rcoset(mxplan), ycntrl(mxplan), ienslp(mxplan), slplst

c     + + + VARIABLE DECLARATIONS + + +

      real           ctlz, ctln, ctlslp, rccoef, rcexp, rcoset, ycntrl,
     1               slplst
      integer        icntrl, ienslp

c     + + + VARIABLE DEFINITIONS + + +

c     icntrl(mxplan) :
c     ctlz(mxplan)   :
c     ctln(mxplan)   :
c     ctlslp(mxplan) :
c     rccoef(mxplan) :
c     rcexp(mxplan)  :
c     rcoset(mxplan) :
c     ycntrl(mxplan) :
c     ienslp(mxplan) :
c     slplst         : downstream value of the channel slope

c     end include file cchtrl.inc

